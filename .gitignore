# Build output (wasm + Go runtime glue live under web/)
/web/bin/
/web/wasm_exec.js

# Dev tooling (built gwc binary lives here; rebuild from the framework checkout)
/.tools/

# Local env / editor
*.log
.DS_Store
.vscode/
