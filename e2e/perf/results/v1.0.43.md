# CashFlux performance ratings — v1.0.43

_Captured 2026-07-15T15:55:10.563Z · 3 passes · 1440x900 · Lighthouse-style log-normal scoring._

**Average page score: 95/100** · Cold-load score: 50/100 · 1 pages rated.

> Methodology: each page is measured as a **warm SPA navigation** from the dashboard — the cost of *arriving on that page* with the wasm runtime already booted. Metrics come from the browser's own Performance timeline (long-task, layout-shift, paint observers), not source instrumentation. The one-time wasm boot is scored separately under **Cold load**. CPU-time metrics (mount/TBT/settle) use **best-of-3** — OS-scheduler contention can only add time, so the fastest pass reflects intrinsic render cost; CLS/DOM use the median.

## Cold load (one-time wasm boot)

Score: **50/100** █████░░░░░

| Metric | Value | Sub-score | Rating |
|---|---:|---:|---|
| Time to interactive | 4479 ms | 44 | poor |
| First Contentful Paint | 44 ms | 100 | good |
| Largest Contentful Paint | 964 ms | 94 | good |
| Blocking time (boot) | 4443 ms | 1 | poor |
| Transfer weight | 19.8 MB | 20 | poor |

Cold-load transfer is **19.8 MB** (wasm alone 16.1 MB), served uncompressed by the local harness. On a real network this is the dominant cost; gzip/brotli on the host typically cuts the wasm 3–4×, but the binary size is the lever that matters.

## Per-page ratings

| Page | Score | Grade | Mount | Blocking | Settle | CLS | DOM |
|---|---:|:--:|---:|---:|---:|---:|---:|
| `/todo` | 95 | A | 110 ms | 53 ms | 124.2 ms | 0.001 | 739 |

## Detailed analysis

### `/todo` — 95/100 A  ██████████

Overall **95/100 (A)**. Weakest signal: **DOM size** at 739 (good) — Interactive + element nodes in the page body — render weight the browser must lay out. Strong: Route mount, Blocking time, Settle time, Layout shift, DOM size.

