# CashFlux performance ratings — v1.0.44

_Captured 2026-07-15T17:44:37.403Z · 5 passes · 1440x900 · Lighthouse-style log-normal scoring._

**Average page score: 88/100** · Cold-load score: 50/100 · 1 pages rated.

> Methodology: each page is measured as a **warm SPA navigation** from the dashboard — the cost of *arriving on that page* with the wasm runtime already booted. Metrics come from the browser's own Performance timeline (long-task, layout-shift, paint observers), not source instrumentation. The one-time wasm boot is scored separately under **Cold load**. CPU-time metrics (mount/TBT/settle) use **best-of-5** — OS-scheduler contention can only add time, so the fastest pass reflects intrinsic render cost; CLS/DOM use the median.

## Cold load (one-time wasm boot)

Score: **50/100** █████░░░░░

| Metric | Value | Sub-score | Rating |
|---|---:|---:|---|
| Time to interactive | 4700 ms | 42 | poor |
| First Contentful Paint | 52 ms | 100 | good |
| Largest Contentful Paint | 976 ms | 94 | good |
| Blocking time (boot) | 4721 ms | 1 | poor |
| Transfer weight | 19.9 MB | 20 | poor |

Cold-load transfer is **19.9 MB** (wasm alone 16.2 MB), served uncompressed by the local harness. On a real network this is the dominant cost; gzip/brotli on the host typically cuts the wasm 3–4×, but the binary size is the lever that matters.

## Per-page ratings

| Page | Score | Grade | Mount | Blocking | Settle | CLS | DOM |
|---|---:|:--:|---:|---:|---:|---:|---:|
| `/transactions` | 88 | B | 147.2 ms | 141 ms | 152.9 ms | 0 | 884 |

## Detailed analysis

### `/transactions` — 88/100 B  █████████░

Overall **88/100 (B)**. Weakest signal: **DOM size** at 884 (needs improvement) — Interactive + element nodes in the page body — render weight the browser must lay out. Strong: Settle time, Layout shift.

